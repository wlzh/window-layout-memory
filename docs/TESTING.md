# 测试与验收报告

2026-09-07 · v0.1.0-preview.1。**不是完整验收通过报告。**

## 已执行

| 检查 | 结果 | 分母/范围 |
| --- | --- | --- |
| 核心回归 | 71通过、0失败、1,634断言 | 合成几何/匹配/存储/保护条件 |
| 核心行覆盖率 | 98.41%，252行中4行未覆盖 | 仅LayoutCore/Core.swift |
| 核心区域覆盖率 | 95.24% | LLVM region，不是分支覆盖 |
| 核心函数执行率 | 96.26% | 107函数中4未执行 |
| 分支覆盖率 | 未测 | 报告无有效分母，不写100% |
| Debug/release构建 | 通过 | Swift6.1 / macOS15.6 / arm64 |
| 版本、plist、git diff | 通过 | 元数据检查，不计功能用例 |
| ad-hoc签名、安装与启动 | 通过 | 不代表Developer ID公证 |
| 三屏读取与权限面板 | 通过 | 只读，不等于恢复或插拔测试 |
| GitHub CI | 通过 | [运行34127463958](https://github.com/wlzh/window-layout-memory/actions/runs/34127463958)，构建/核心测试/覆盖率 |
| GitHub发行资产回下载 | SHA256与unzip完整性通过 | v0.1.0-preview.1，非公证检查 |

发行归档SHA256：`1ee56b387515a20e10863b473f1977b05c095113aaa9e8b6109074c0e6b5e395`。
本机安装与构建二进制SHA256均为`0cd9283d16232411ec00f6d221d10794c0ddcfb009ae1e95a0a5f2fb38aa4185`。

Engine、AX服务与UI不在98.41%分母内。不得把核心覆盖率当作全应用覆盖率。

## 执行命令

```sh
zsh scripts/test-all.sh
zsh scripts/coverage.sh
zsh scripts/package-release.sh
swift scripts/measure-idle.swift <PID> 600
```

无需XCTest；覆盖率脚本通过Swift插桩与llvm-cov测量并要求核心行覆盖≥90%。临时测试数据全部合成，退出时清理。

## 42项PRD追踪

表中“核心通过”只覆盖部分A子项。所有H实机门禁尚未整体关闭。

| 条目 | 已有证据 | 尚需验证 |
| --- | --- | --- |
| T01-T08 | 拓扑隔离、枚举不变、镜像拒绝、负坐标核心通过；读取3屏 | 实际切屏、缩放、线缆、镜像 |
| T09-T13 | 小窗口几何合法；仅前台AX候选实现 | 台前调度、隐藏、最小化及焦点保持 |
| T14-T17 | 保护条件、修订和历史核心通过 | Engine事件、锁定/关闭与GUI竞态 |
| T18 | 无线索不猜匹配 | 登录和延迟窗口 |
| T19-T21 | 歧义、跨应用隔离、会话绑定核心通过 | 实际应用身份与角色UI |
| T22-T25 | 权限/暂停/睡眠/代际核心通过 | AX中途撤销、部分写入与撤销E2E |
| T26-T28 | schema、损坏、符号链接、大小限制、原子替换、不重复写核心通过 | 磁盘满/断电与实际升级、导入UI |
| T29-T30 | 离屏可达与无效几何核心通过 | 应用尺寸限制、无响应批次 |
| T31-T32 | 安装、启动、版本和签名通过 | 登录注册、完整卸载、其它系统 |
| T33-T36 | 采样工具与有界工作代码 | 10分钟代表负载、8小时、Engine压力 |
| T37-T42 | 两模式共用保护、小尺寸窗口回归 | 两模式完整E2E和运行中切换 |

## 实机方法

1. 私人备份当前布局；使用可丢弃测试窗口，不拿用户文稿作强制移动试验。
2. 用户授权安装版本，记录真实系统/应用版本与窗口数。
3. 普通桌面下验证采集、保存、移动、恢复、撤销；目标与实际误差各分量≤4pt。
4. 台前调度开启重复，含后台组、小窗口、隐藏/最小化和切换模式。
5. 每个配置先有正确基准，再按1→2→3→2→1循环10轮，错误覆盖必须为0。
6. 登录3次、唤醒10次，含延迟启动应用与多窗口身份变化。
7. 权限撤销、无响应测试应用、同标题歧义应明确失败而不搬错。
8. 两模式分别性能测量，再做长时驻留。

不自动重启用户机器、断开显示器或改台前调度设置，需协调实机操作。

## 性能口径

measure-idle.swift用proc_pid_rusage起止累计值计算单逻辑核CPU、物理footprint、RSS、interrupt和package-idle唤醒。工具自身睡眠，不增加应用轮询。

两点采样不反映中间峰值，也不证明无泄漏；8小时需要多点、峰值和真实工作负载记录。需注明release构建、权限、台前调度模式、监听窗口数、面板状态、采样时长和期间交互。

未授权、零窗口、启动或面板打开状态的读数不能代表20应用/50窗口负载。完整性能门槛尚未验收，不能宣称≤0.1%CPU、≤50MiB或零唤醒已达标。

### 短时工具自检样本

本机release预览、状态面板打开、尚未开始真实AX窗口监听，60.005秒采样：平均单核CPU 0.001521%，physical footprint 31.019MiB，RSS 75.188MiB，interrupt wakeups 0.400/s，package-idle wakeups 0/s。这是空闲工具自检，不是PRD的代表性负载或10分钟/8小时门禁。
