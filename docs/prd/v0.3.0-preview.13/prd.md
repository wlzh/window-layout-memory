# 竖屏横向撑满

已确认：新增独立、默认关闭的竖屏开关，只在台前调度模式生效。左右撑满但左侧沿用已有留白（默认100 pt）；保留纵向位置和高度，越界时必要修正。正方形、真实镜像显示、Spaces和原生全屏不扩展支持。

验收：横竖开关互不依赖；复用例外与子窗口规则；成功记录实际几何及显示器，锁定/失败/取消/歧义不覆盖；旧数据不自动启用新功能；菜单列表不新增窗口，资源不新增常驻轮询。文档、版本、测试和安装结果区分自动化与实机门禁。
