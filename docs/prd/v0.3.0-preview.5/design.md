# 子窗口策略交互

主菜单“横屏自动铺满”下缩进一项“同时铺满子窗口”，默认无勾，总开关关闭时置灰。留白默认100 pt，无单独设置菜单。

“铺满排除”子菜单显示当前已核对窗口的临时排除。存在标识时显示持久排除确认入口；否则显示不能可靠持久识别的提示。确认文本明确标识可能被应用复用，也可能影响主窗口。已有规则按应用与标识列出，可逐条撤销。没有已核对窗口时显示先激活并等待核对的提示，不猜最近窗口。

保持原生AppKit菜单、字号、勾选与禁用状态，沿用明暗主题。状态面板显示子窗口开关、持久规则数和临时排除数，不暴露窗口标题和路径。
