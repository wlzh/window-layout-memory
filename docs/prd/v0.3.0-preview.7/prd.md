# 需求：菜单分组

版本v0.3.0-preview.7，build 11，schema 1。

目标：减少平铺条目，保留高频动作，明确不同排除和暂停作用域。根菜单12项、四个分组、最多两级子菜单。支持已保存但未运行应用的例外撤销。

不改变显示器组合身份、窗口匹配、保存和铺满策略；不新增后台扫描或数据迁移。

验收：动作路由正确，禁用状态不被AppKit覆盖，菜单展开不写盘/扫描/移动，原有回归通过。布局与性能实机验收不得用菜单测试代替。

详见[设计](design.md)、[技术](dev.md)、[计划](plan.md)。
