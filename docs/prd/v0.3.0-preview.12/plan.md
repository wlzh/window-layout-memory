# 交付计划

1. 最终确认：保留原生菜单列表交互，撤回管理弹窗。
2. 实现：统一入口、分组、勾选状态和禁用原因，复用现有规则。
3. 验证：路由/payload/持久化/权限/文件选择/深度回归及版本链接检查。
4. 交付：构建、签名、备份、替换安装、启动核验、Git提交。

风险：无稳定窗口标识时只能临时排除；ad-hoc签名的新二进制可能需用户重新授权；真实硬件和8小时性能不因菜单改组而视为通过。build 18管理面板已撤回，build 19替换为轻量列表。结果见[测试报告](../../TESTING.md)。
