# 铺满例外统一管理

最终需求：合并应用与窗口例外至原生菜单列表，不弹管理窗口。应用例外永久保存；窗口例外明确临时与永久区别。已有选择不丢失，文件选择和未运行应用取消仍可用。

验收：单一“铺满例外”子菜单，在同一列表内分组，不增加菜单层级；空态和不可用原因可见；永久规则可撤销；普通布局数据、显示器组合、留白和铺满策略不变；打开菜单不扫描、不写偏好、不移动窗口。回归、版本、说明、安装与提交需有证据；尚未完成的硬件或长期性能单独列出。
