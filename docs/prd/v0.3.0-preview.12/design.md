# 原生菜单交互

入口：台前调度铺满 → 铺满例外。一个原生菜单列表按分隔线分为应用级、当前窗口、已添加的窗口例外，不创建设置窗口、不再嵌套子菜单。长列表由系统菜单滚动呈现，保留应用图标和同名应用标识。

应用级标题注明永久保存；当前窗口提供临时勾选和永久标识排除；已添加窗口规则前缀为临时或永久，点击取消。灰色标题是分组说明，空列表显示明确文案。授权缺失、暂停、核对中及无标识分别说明。

仅“永久排除此类窗口…”保留安全确认，“从文件选择应用…”保留标准文件选择器，其它例外直接点击切换。管理面板实验已撤回，不再保留截图或弹窗入口。
