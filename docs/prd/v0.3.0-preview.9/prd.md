# 需求：已对齐边保持不动

build 14，schema 1。当前横屏中，窗口已经贴边的方向保持不动，未到位的边向目标延展；左侧为按组合保存的留白，默认100 pt。禁止先将整个窗口搬到最终左侧再放大，禁止原生最大化回退。

授权后下次应用事件应恢复核对，不要求用户额外点击刷新。保留其它组合布局、应用例外、子窗口策略及普通恢复。失败应停止并如实报告，不承诺所有应用均支持或绝对无中间闪动。

验收覆盖四边、收缩、扩展、负坐标、已到位、应用限制、延迟、取消、权限变更及真实应用观测；测试不能替代实机验收。
