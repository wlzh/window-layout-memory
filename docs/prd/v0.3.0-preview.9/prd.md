# 需求：支持的窗口铺满，不支持的窗口不先搬动

build 15候选，schema 1。用户要求是避免尺寸受限窗口被提前搬到左侧后失败抖动，并非要求强制扩展每个窗口。

保留正常支持窗口的横屏铺满和按组合留白（默认100 pt）；先确认目标尺寸可达到，再写入最终位置。不支持时停止，不使用原生最大化，不分步反复挪动。保留应用例外、子窗口策略、普通布局恢复和已有基准。

授权后下一次应用事件恢复核对。模拟回归与真实应用验收分开记录；未通过不发布为已验证修复。
