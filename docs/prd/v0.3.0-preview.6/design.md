# 应用例外交互

菜单“横屏铺满排除应用”列出当前运行应用，勾选表示排除。分隔线下保留已排除但未运行的应用，标注“未运行”，可直接取消。底部“从文件选择应用…”打开原生单选.app文件选择器，默认Applications目录，不启动应用。无有效应用标识时显示错误，不保存。

旧的全局入口改为“排除应用（全部布局功能）”，避免与只排除铺满混淆。沿用原生菜单、勾选、禁用和明暗主题。状态面板显示排除应用数量。

边界回退无需额外开关，失败不持续重试。说明明确原生缩放自身也可能改变位置/大小，不把它描述为原子且完全无跳动操作。
