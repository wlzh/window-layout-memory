# 应用例外与边界回退需求

支持从运行应用列表或文件选择.app加入横屏铺满例外，选择后跨应用/工具重启保留，未运行应用也可取消，不依赖安装路径，不清除原布局记录。与停止全部布局功能的全局排除分开。

解决主窗口因为原位置无法容纳目标尺寸而无法铺满的问题，但不能重新引入失败前盲目左移，不能进入原生全屏，也不能持续循环抢窗口。保留子窗口选项和100 pt默认值。

验收：保存/重载/撤销/重复ID/非法文件/上限保护均测试；原生缩放必须有明确动作才执行，有限等待、再次失败或取消后停止。真实Telegram/微信验收与合成测试分开报告。
