# v0.3.0-preview.13 / build 20

新增“台前调度铺满 → 竖屏横向撑满”，默认关闭。左侧沿用当前组合/显示器留白，右侧贴可用区；保持纵向位置和高度，越界才修正。与横屏开关独立，复用同一应用/窗口例外和子窗口策略。

成功核验后保存实际布局，手动换屏优先；失败、取消、歧义、锁定不覆盖。兼容旧schema 1，新字段缺省false；导入关闭两个方向自动撑满。不支持实际显示器镜像、正方形、Spaces或原生全屏。

保持原生菜单列表，无新增管理窗口和后台轮询。自动测试、安装及未完成实机门禁见[测试](../TESTING.md)，开发预览不代表所有应用和硬件已通过。
