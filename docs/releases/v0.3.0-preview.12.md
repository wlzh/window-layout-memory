# v0.3.0-preview.12 / build 19

应用及窗口例外合并到“台前调度铺满 → 铺满例外”原生菜单列表。最终交互不弹管理窗口、不增加子菜单层级。已有应用例外继续永久保留，可从运行列表或.app文件添加，未运行应用仍可取消。窗口级区分本次运行临时规则与跨重启标识规则，空态和不可用原因可见。

保留schema 1、布局、历史、显示器组合、100 pt留白、铺满结果保存及手动换屏优先。永久标识可能被复用，不按标题猜测；添加时保留确认。例外不会删除布局，铺满模式中的防旁路恢复保护保持不变。没有新增管理窗口、计时器或后台轮询。build 18弹窗实验已撤回，不作为交付方案。

测试、安装及剩余门禁见[测试报告](../TESTING.md)。开发预览，未宣称全部应用、硬件或长期性能通过；本地安装和Git提交不等于GitHub Release。
