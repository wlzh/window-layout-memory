# v0.3.0-preview.9

2026-09-09 · build 14 · schema 1 · 开发预览。

- 同屏横屏铺满改为小步边缘扩展，保留已对齐边，不先整窗移动到最终左侧，不调用原生最大化。
- 修复授权后监控仍使用旧未授权状态的问题；下一次应用事件自动重新核对，无新增轮询。
- 保留100 pt及按组合记忆的自定义值、应用/窗口例外、菜单和普通恢复。跨屏铺满仍采用原有尺寸核验策略。

AX位置与尺寸分开写，仍可能短暂抖动或部分调整；失败、取消、超时即停止，不覆盖用户操作。并非所有第三方应用兼容承诺。完整规则见[铺满说明](../STAGE_FILL.md)，验证及安装进度见[测试](../TESTING.md)和[状态](../STATUS.md)。arm64、ad-hoc签名、未公证，部署目标macOS13。
