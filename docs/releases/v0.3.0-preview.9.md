# v0.3.0-preview.9

2026-09-09 · build 15候选 · schema 1 · 尚未发布。

撤回build 13/14的分步挪动实验。当前策略先核验目标尺寸，再写位置，保留100 pt或已记忆留白；尺寸未达到时停止，不先搬到左侧、不原生最大化。尺寸失败不能等同于应用不支持：原位置受屏幕边界限制时，也可能无法通过门禁。

**实机未通过**：已恢复授权，但Arc激活后仍未达到目标。新增兼容处理没有完成该问题的验收；此候选不可作为已修复版本发布。详细坐标、测试边界见下方测试与状态链接。

新增事务级AXEnhancedUserInterface兼容处理：仅原值true且系统辅助技术未开启时临时处理，结束恢复，恢复失败可见。授权变化在下一次应用事件自动重新核对。菜单、应用例外、组合基准和普通恢复不变，无新运行时依赖。

AX请求仍由各应用实现，不能保证所有应用支持或零闪动；应用自身对尺寸请求的反应不等于工具主动写位置。实际验证见[测试](../TESTING.md)和[状态](../STATUS.md)。arm64、ad-hoc签名、未公证，部署目标macOS13。
