# v0.2.0-preview.1

2026-09-08，build 3，MIT，arm64，ad-hoc签名，未公证。数据schema保持1。

发行归档：`WindowLayoutMemory-v0.2.0-preview.1-macos-arm64.zip`

SHA256：`9798779a5e813348cad9ee082314c41ead683d82e228e681a2f017721a5c4c1a`，清单文件`SHA256-v0.2.0-preview.1.txt`。

## 新增

- 菜单栏只读布局预览，当前/保存/对比视图。
- 实际比例的多屏排列、窗口尺寸和坐标详情，列表选择及重叠循环选择。
- 完整显示器组合隔离；其它组合只能查看其自身保存布局。
- 唯一匹配的几何差值、读取时间、后台/歧义提示；不采集屏幕内容。
- 事件合并刷新、无新增周期扫描、关闭后释放预览对象。
- 版本读取统一，新增核心、协调器和AppKit生命周期回归。

## 验证边界

自动化、合成AppKit渲染、真实窗口AX读写、多显示器插拔和长时性能是不同门禁。具体结果见仓库docs/TESTING.md，不能将单元测试通过理解为整个软件所有场景已验收。

本地台前调度关闭状态下：114项核心、32项协调器、12项AppKit检查通过；核心行覆盖99.04%。构建、签名及压缩包完整性通过。锁屏中断后已恢复台前调度开启，新版已重新授权并读取真实窗口。用户实际三屏转单屏后，单屏被识别为独立的未建基准组合，三屏8窗口记录与升级前备份一致；尚未验证接回恢复、ON/OFF完整应用流程及长时性能。

## 升级

退出旧版再安装，保留现有布局。临时签名变化可能要求重新添加本应用辅助功能授权，由用户完成系统认证；不要清空其它应用权限。首次从菜单打开预览，无需录屏权限。
