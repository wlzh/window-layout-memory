# v0.3.0-preview.10

2026-09-09 · build 16候选 · schema 1 · 尚未发布。

按用户最新要求，先对齐左侧留白再调整目标尺寸，不记录或判断历史成功。默认100 pt及已记忆留白、子窗口与应用例外保持不变。

尺寸失败停止报告，不原生最大化、不自动回滚、不循环重试。受限窗口仍可能移动后未铺满，不保证所有应用兼容或零抖动。

无新菜单、依赖、缓存、定时器或数据迁移。普通布局恢复不变。实机及发布门禁见[状态](../STATUS.md)和[测试](../TESTING.md)。arm64、ad-hoc签名、未公证，最低macOS13。
