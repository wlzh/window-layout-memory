# v0.3.0-preview.3

2026-09-09，build 7，schema 1，MIT；arm64、ad-hoc签名、未公证。

修复“窗口被挤到内屏后只在内屏铺满”：自动恢复开启且窗口唯一匹配当前组合基准时，先使用记录中的目标显示器。横屏应用该屏保存留白，竖屏恢复原几何；不借用其它组合。未记录、匹配歧义或关闭自动恢复时不猜测跨屏，仍允许本屏横屏铺满。

手动拖动不立即搬回，下一次激活重新应用保存归属。合入铺满后保护期内激活请求不丢失的修复。原布局基准和历史不因铺满修改。

仍只处理已激活、可访问的前台普通窗口，不展开后台台前调度组。未记录的应用无法凭空恢复，微信第二实例尺寸写入仍有已知失败，不能保证所有应用完整恢复。完整真实拔插、重启与长时性能仍未全部验收。

测试与安装状态见[测试报告](https://github.com/wlzh/window-layout-memory/blob/main/docs/TESTING.md)和[当前状态](https://github.com/wlzh/window-layout-memory/blob/main/docs/STATUS.md)。升级前备份并退出旧版，保持已有偏好；新签名可能要求用户重新添加辅助功能授权。

本地228项自动化检查通过；release构建、签名和元数据检查通过。已安装build 7，升级前后私人布局文件逐字节一致；初装新签名等待重新授权，尚未把真实拔插恢复标为通过。
